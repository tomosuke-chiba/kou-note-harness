{
  "name": "note-harness",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source self-hosted static blog with content gating — note代替",
  "scripts": {
    "dev:worker": "pnpm --filter worker dev",
    "build": "pnpm -r build",
    "build:blog": "pnpm --filter blog build",
    "deploy:worker": "pnpm --filter worker deploy",
    "db:migrate": "pnpm --filter worker exec wrangler d1 execute note-harness --file=../../packages/db/schema.sql",
    "db:migrate:local": "pnpm --filter worker exec wrangler d1 execute note-harness --file=../../packages/db/schema.sql --local",
    "typecheck": "pnpm -r typecheck"
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "license": "MIT"
}
